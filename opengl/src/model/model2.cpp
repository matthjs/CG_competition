#include "model.ih"

/*
namespace
{
    QVector<QVector3D> locate3D(size_t pos) const
    {

    }

    QVector<QVector2D> locate2D(size_t pos) const
    {

    }
}
*.

/**
 * @brief Model::Model Reads raw data directly.
 */

//Model::Model(QString const &rawfile, bool dummy)
//{
    //ifstream in(rawfile, ios::in | ios::binary);
    //size_t size;

    //ifstream in(vertices);
    //in >> size;

    //in.read(reinterpret_cast<const char *>(&vertices_indexed[0]), )
//}
